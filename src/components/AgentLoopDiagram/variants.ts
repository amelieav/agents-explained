import { AgentLoopDiagramVariant } from "./types";

export const agentLoopVariantClass: Record<AgentLoopDiagramVariant, string> = {
  default: "agent-loop--default",
  compact: "agent-loop--compact"
};
