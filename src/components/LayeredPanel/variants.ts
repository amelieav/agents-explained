import { LayeredPanelVariant } from "./types";

export const layeredPanelVariantClass: Record<LayeredPanelVariant, string> = {
  soft: "layered-panel--soft",
  glass: "layered-panel--glass",
  highlight: "layered-panel--highlight"
};
