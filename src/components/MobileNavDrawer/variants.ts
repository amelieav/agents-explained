import { MobileNavDrawerVariant } from "./types";

export const mobileDrawerVariantClass: Record<MobileNavDrawerVariant, string> = {
  pastel: "mobile-drawer--pastel",
  subtle: "mobile-drawer--subtle"
};
